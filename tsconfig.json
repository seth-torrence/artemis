{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Solution file. Contains no inputs of its own — it only wires the project graph together so `tsc -b` at the repo root typechecks everything in dependency order.",
  "files": [],
  "references": [
    { "path": "./packages/protocol" },
    { "path": "./packages/transcript" },
    { "path": "./packages/core" },
    { "path": "./packages/sdk" },
    { "path": "./apps/server" },
    { "path": "./apps/tui" },
    { "path": "./apps/desktop/main" },
    { "path": "./apps/desktop/preload" },
    { "path": "./apps/desktop/renderer" },
    { "path": "./scripts" }
  ]
}
